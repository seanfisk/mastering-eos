% -*- coding: utf-8; -*-

% \RequirePackage[l2tabu,orthodox]{nag}
% Note: Font size doesn't do anything here. Use scale in the beamerposter usepackage below.
\documentclass{beamer}
\mode<presentation>
{
  \usetheme{EOS}
}
\graphicspath{{images/}}

\usepackage{minted}
\newmint{bash}{frame=single}
\newminted{bash}{gobble=6,frame=single}

% Just loading fontspec should cause LuaLaTeX to pick a font that can display most Unicode characters.
\usepackage{fontspec}
\usepackage{tabu}
% Add more space to the top and bottom of cells.
\tabulinesep=0.8em

\usepackage{microtype}
% We want 4' X 3' (or 48" X 36") landscape, but these measurements are in centimeters.
\usepackage[orientation=landscape,size=custom,width=121.92,height=91.44,scale=1.89]{beamerposter}

\usepackage{changepage} % For the adjustwidth environment
\usepackage{wrapfig}

\newcommand{\mailtohref}[1]{\href{mailto:#1}{\nolinkurl{#1}}}
\newcommand{\clicode}[1]{\texttt{#1}}

% Itemize environments and minted code blocks do not follow the block indent, so we do it manually. It's a hack to be sure, but it works.
\newlength{\blockinnerindent}
\setlength{\blockinnerindent}{2em}
% Wasn't able to succeed in making an environment that does bashcode and indenting. Seems to be some problem with environments and minted.
\newenvironment{indented}{\bigskip\begin{adjustwidth}{\blockinnerindent}{}}{\end{adjustwidth}}
\newenvironment{indented_itemize}{\begin{adjustwidth}{\blockinnerindent}{}\begin{itemize}}{\end{itemize}\end{adjustwidth}}

\title{Mastering EOS}
\author[Fisk \& Woodring]{
  Sean Fisk~~\mailtohref{<fiskse@mail.gvsu.edu>}}
  % Ira Woodring\dotfill\mailtohref{woodriir@gvsu.edu}

\begin{document}
\begin{frame}[fragile]{}
  \begin{columns}[T]
    \begin{column}{0.33\textwidth}
      \input{blocks/manip_path.tex}
      \input{blocks/multiple_terminals.tex}
      \input{blocks/dir_nav.tex}
      \input{blocks/vcs.tex}
    \end{column}
    \begin{column}{0.33\textwidth}
      \input{blocks/downloading.tex}
      \input{blocks/installing_software.tex}
      \input{blocks/build_systems.tex}
      \input{blocks/parallel_make.tex}
    \end{column}
    \begin{column}{0.33\textwidth}
      \input{blocks/inter_eos_ssh.tex}
      \input{blocks/clipboard.tex}
      \input{blocks/users.tex}
      \input{blocks/getting_help.tex}
      \input{blocks/misc.tex}
    \end{column}
  \end{columns}
\end{frame}

\end{document}
